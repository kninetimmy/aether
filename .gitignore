.memhub/
agent_docs/PROJECT.md
agent_docs/PROJECT_LEDGER.md
.claude/settings.local.json

# Node / frontend
node_modules/
frontend/dist/
*.tsbuildinfo

# Operator-local config (station coords, API keys, toggles) — never committed
# (PRD §5). The committed template is `.env.example`; run.sh copies it to `.env`.
.env

# run.sh runtime dir: broker log + PID files (operator-local)
.aether/

# SQLite persistence store + WAL sidecars (M4; operator-local, never committed)
*.db
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
